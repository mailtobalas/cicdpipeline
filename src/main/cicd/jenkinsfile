pipeline {
    agent any

    environment {
        mavenHome = tool 'MAVEN'
    }



    stages {

        stage('Build'){
            steps {
                echo mavenHome
                bat "mvn clean install -DskipTests"
            }
        }


    }
}